import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export function GuardGateScreen() {
  return (
    <PlaceholderScreen
      title="Gate console"
      subtitle="Low-bandwidth UI: verify passcodes, search units, and trigger resident approve/deny in realtime."
    />
  );
}
