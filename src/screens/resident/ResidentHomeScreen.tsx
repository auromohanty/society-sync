import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export function ResidentHomeScreen() {
  return (
    <PlaceholderScreen
      title="Resident home"
      subtitle="View maintenance bills, pay dues, and pre-approve visitors from this hub."
    />
  );
}
