import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export function PreApproveGuestScreen() {
  return (
    <PlaceholderScreen
      title="Pre-approve guest"
      subtitle="Generate a 6-digit passcode (24h validity) and notify security when a visitor arrives."
    />
  );
}
